intent_id: INT-001
owner: your-handle
status: active

slo:
  lead_time_p95_hours: 24
  mttr_p95_minutes: 60
  change_failure_rate_max: 0.2

permissions:
  allow:
    - open_issue
    - open_pr
    - generate_report
  deny:
    - merge
    - tag_release
    - write_src

code_ownership:
  approvals_required: 1
  protected_paths:
    - "src/**"
    - "infrastructure/**"
    - "secrets/**"
  safe_paths:
    - "docs/**"
    - "tests/**"
    - "reports/**"
    - "logs/**"

rate_limits:
  api_rpm_max: 30
  llm_tokens_day_max: 150k
